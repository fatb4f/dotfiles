# Hookrail Codex config wiring

Hookrail is superseded by Frame. Keep this config only for rollback.

The hook scope stays the same, but the active command target is now:

```text
$CODEX_HOME/tools/frame/bin/frame
```

Use `config/hookrail.config.toml` only when you need the legacy tree for a
temporary compatibility install.
