# Semgrep Patterns

Use `semgrep` for reusable rule packs and taint/security checks.

- Prefer semantic queries over ad-hoc text search.
- Use `--config auto` for broad scans.
- Use named rules when the question is security-oriented.
