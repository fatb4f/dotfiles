if (( x > 0 && x < 100 )); then
    echo "In range"
fi

while (( i++ < 10 )); do
    (( sum += i ))
done

(( result = a > b ? a : b ))
(( flag = !flag ))
(( bits = (1 << n) | mask ))
(( value = base ** exp % mod ))

[[ $((x % 2)) -eq 0 ]] && echo "Even"
