# ShellSpec instructions

Use ShellSpec for sourceable shell functions and helper logic.

Prefer direct function calls with `Describe`, `Context`, and `It` blocks. Use Bats instead for generated executable behavior and CLI argv contracts.
