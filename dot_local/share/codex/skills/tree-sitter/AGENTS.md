# tree-sitter instructions

Use tree-sitter-cli for optional structural inspection evidence such as functions, ranges, or syntax shape.

Do not build a runtime selector engine from this skill. Use it as inspection support for the agent workflow.
