# Ripgrep Patterns

Use `rg` for live-filesystem text search.

- Prefer `-n --no-heading`.
- Add `-t <lang>` or path globs to narrow scope.
- Use `-F` for literal strings and `-i` only when casing is uncertain.
