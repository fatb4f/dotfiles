# fd Guide

Use `fd` for path discovery.

- Search by filename or glob before reading content.
- Quote globs so the shell does not expand them first.
- Combine with `rg -X` when you need to search matching files.
