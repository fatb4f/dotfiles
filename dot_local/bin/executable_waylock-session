#!/usr/bin/env bash
set -euo pipefail

WAYLOCK_BIN=/usr/bin/waylock
WAYLOCK_ARGS=(
  -ignore-empty-password
  -init-color 0x000000
  -input-color 0x222222
  -input-alt-color 0x333333
  -fail-color 0x550000
)

exec "$WAYLOCK_BIN" "${WAYLOCK_ARGS[@]}"
