# ~/.zprofile
# Login shell only. Universal env has already been resolved by .zshenv.

if command -v uwsm >/dev/null 2>&1 && uwsm check may-start >/dev/null 2>&1; then
  exec systemd-cat -t uwsm_start uwsm start niri.desktop
fi
