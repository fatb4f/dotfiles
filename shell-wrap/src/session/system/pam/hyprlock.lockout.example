# PAM fragment for hyprlock break lockout enforcement.
#
# Install this line before the normal password auth stack in /etc/pam.d/hyprlock.
# PAM must call the root-owned compatibility wrapper, not a user-home projection.

auth requisite pam_exec.so quiet /usr/local/libexec/pam-pomodoro-gate
auth include system-auth
